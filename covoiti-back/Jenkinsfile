pipeline {
    agent any
    
    stages {
       stage {
          steps {
            
          }
       }
    }

    post {
        success {
            echo 'Build and tests succeeded!'
        }
        failure {
            echo 'Build or tests failed.'
        }
    }
}
